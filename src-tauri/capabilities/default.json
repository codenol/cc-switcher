{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the settings window",
  "windows": ["settings", "main", "prompt"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "opener:default"
  ]
}
